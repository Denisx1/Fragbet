export enum RolesEnum {
  ADMIN = "ADMIN",
  USER = "USER",
  SUPER_ADMIN = "SUPER_ADMIN",
  MANAGER = "MANAGER",
}

export enum RoleLevel {
  USER = 1,
  MANAGER = 2,
  ADMIN = 3,
  SUPER_ADMIN = 4,
}
